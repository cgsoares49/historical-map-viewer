shooter/
New Compressed (zipped) Folder.zip
dist/
.wrangler/
.claude/worktrees/

# ── CREATOR private files (local only) ───────────────────────────────────────
mapper/mapper-local.html

# ── Tile data directories (local only — not committed to GitHub) ─────────────
mapper/rivers/
mapper/counties/
mapper/polareas/
mapper/pols/
mapper/coasts/
mapper/niw/
mapper/cities/
mapper/inwaters/
mapper/primaries.txt
mapper/geodata_from_par.csv
mapper/coasts/125/CST020old.PRN
rivers/
counties/

# ── VB.NET build artifacts ───────────────────────────────────────────────────
mapper.NET.dll
mapper.NET.exe
mapper.NET.pdb
mapper.NET.deps.json
mapper.NET.runtimeconfig.json
mapper.NET.runtimeconfig.dev.json

# ── Python cache ─────────────────────────────────────────────────────────────
mapper/__pycache__/

# ── Misc data / work files ───────────────────────────────────────────────────
Work Area/
NVALUES.DAT
offsets.txt
mapper.ini
make_shortcut.vbs
*.bmp
